const AuthController = require('./AuthController');
const SummarizeController = require('./SummarizeController');
const UserController = require('./UserController');

module.exports = {
  AuthController,
  SummarizeController,
  UserController,
};
